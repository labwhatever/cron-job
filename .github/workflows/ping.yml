name: Keep Render Alive
on:
  schedule:
    - cron: '*/5 * * * *' 
  workflow_dispatch: 

jobs:
  ping-backend:
    runs-on: ubuntu-latest
    steps:
      - name: 🔁 Ping Render backend
        run: |
